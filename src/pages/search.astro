---
// src/pages/search.astro (已修正)
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// (這是你 index.astro 裡的函式，我們複製過來用)
const formatDate = (date: Date) => {
  const d = new Date(date);
  return `${d.getFullYear()}/${String(d.getMonth() + 1).padStart(2, '0')}/${String(d.getDate()).padStart(2, '0')}`;
};

// 1. 取得搜尋參數 (你的邏輯是正確的)
const displayQuery = Astro.url.searchParams.get('q') || null;
const searchQuery = displayQuery?.toLowerCase() || null;

let filteredPosts = [];

if (searchQuery) {
  const allPosts = await getCollection('blog');
  filteredPosts = allPosts.filter((post) => {
    const title = post.data.title?.toLowerCase() || '';
    const description = post.data.description?.toLowerCase() || '';
    const body = post.body?.toLowerCase() || '';
    return (
      title.includes(searchQuery) ||
      description.includes(searchQuery) ||
      body.includes(searchQuery)
    );
  });
}

const pageTitle = searchQuery ? `搜尋「${searchQuery}」的結果` : '搜尋文章';
---
<BaseLayout pageTitle={pageTitle} description={`在 KAE的雜記 中搜尋文章`}>
  <main class="max-w-3xl mx-auto px-4 sm:px-6 py-12">
    <h1 class="text-3xl font-bold text-[var(--text-primary)] mb-6">搜尋文章</h1>

        <form
      action="/search"
      method="GET"
      class="flex items-center space-x-2 mb-10"
    >
      <input
        type="text"
        name="q"
        value={displayQuery || ''}
        placeholder="搜尋文章..."
        class="w-full px-3 py-2 rounded-md border border-[var(--border-color)] text-sm text-[var(--text-primary)] bg-white focus:outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
      />
      <button
        type="submit"
        class="px-4 py-2 bg-[var(--accent-color)] text-white text-sm rounded-md hover:bg-[var(--accent-hover)] transition"
      >
        搜尋
      </button>
    </form>

    {searchQuery ? (
      <>
        <p class="text-lg text-[var(--text-secondary)] mb-8">
          找到 <span class="font-semibold text-[var(--text-primary)]">{filteredPosts.length}</span> 篇關於「<span class="font-bold text-[var(--text-primary)]">{displayQuery}</span>」的文章：
        </p>

        {filteredPosts.length > 0 ? (
                    <div class="space-y-4">
            {filteredPosts.map(post => (
              <article class="bg-[var(--bg-card)] rounded-xl shadow-md flex items-center p-4 hover:shadow-lg transition-all duration-300">
                <div class="w-24 h-24 flex-shrink-0 overflow-hidden border border-[var(--border-color)] mr-4 rounded-lg">
                  <img src={post.data.previewImage} alt={post.data.title} class="w-full h-full object-cover" />
                </div>
                <div class="flex-1 flex flex-col">
                    <div class="mb-2 flex flex-wrap gap-1 justify-end">
                      {post.data.category.map(cat => (
                        <a href={`/category/${cat}`} class="text-xs font-medium text-white bg-[var(--accent-color)] py-1 px-2 rounded-full hover:bg-[var(--accent-hover)] transition-colors">
                          {cat}
                        </a>
                      ))}
                    </div>
                    <div class="flex-grow">
                      <h3 class="text-lg font-bold text-[var(--text-primary)] mb-1 line-clamp-2">
                        <a href={`/blog/${post.slug}`} class="hover:text-[var(--accent-color)] transition-colors whitespace-pre-line">
                          {post.data.title}
                        </a>
                      </h3>
                      <p class="text-sm text-[var(--text-secondary)] line-clamp-2 mb-2">{post.data.description}</p>
                    </div>
                    <div class="flex flex-wrap items-center justify-between text-xs text-gray-500 gap-x-2 gap-y-1">
                      <div class="flex flex-wrap items-center gap-x-2 gap-y-1">
                        <span>{formatDate(post.data.publishDate)}</span>
                        {post.data.tags?.slice(0, 3).map(tag => (
                          <span class="px-2 py-0.5 rounded-md text-white text-xs" style="background-color: var(--accent-color);">
                            {tag}
                          </span>
                        ))}
                      </div>
                      <a href={`/blog/${post.slug}`} class="text-[var(--accent-color)] hover:underline ml-auto">閱讀 &rarr;</a>
                    </div>
                  </div>
                </article>
            ))}
          </div>
        ) : (
          <p class="text-center text-gray-500 text-lg py-10">
            找不到任何符合「<span class="font-bold text-[var(--text-primary)]">{displayQuery}</span>」的文章 😅
          </p>
        )}
      </>
    ) : (
      <p class="text-center text-gray-500 text-lg py-10">
        請在上方的搜尋框輸入關鍵字。
      </p>
    )}
  </main>
</BaseLayout>
