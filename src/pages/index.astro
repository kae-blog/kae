---
// src/pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import Sidebar from '../components/Sidebar.astro';
import { getCollection } from 'astro:content';

// ------------------------------------
// 取得所有文章
// ------------------------------------
const allPosts = await getCollection('blog');

// 按發佈日期排序（最新在前）
allPosts.sort(
  (a, b) => new Date(b.data.publishDate).getTime() - new Date(a.data.publishDate).getTime()
);

// ------------------------------------
// 精選文章（category 或 tags 包含 '精選文章'）
// ------------------------------------
const featuredPosts = allPosts.filter(post => {
  const categories = Array.isArray(post.data.category) ? post.data.category : [];
  const tags = Array.isArray(post.data.tags) ? post.data.tags : [];
  return categories.includes('精選文章') || tags.includes('精選文章');
}).slice(0, 6); // 只取最新6篇
---

<BaseLayout>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-10 lg:gap-12">

    <!-- 📰 主內容區 -->
    <div class="md:col-span-2 space-y-12">

      <!-- 最新文章區塊 -->
      <section>
        <h2 class="text-3xl font-bold text-[var(--text-primary)] mb-6 pb-3 border-b border-[var(--border-color)]">
          最新文章
        </h2>

        <div class="space-y-4">
          {allPosts.map(post => (
            <article class="bg-[var(--bg-card)] rounded-xl shadow-md flex items-center p-4 hover:shadow-lg transition-all duration-300">

              <!-- 左側縮圖：使用 previewImage 1:1 -->
              <div class="w-24 h-24 flex-shrink-0 overflow-hidden border border-[var(--border-color)] mr-4 rounded-lg">
                <img src={post.data.previewImage || '/images/default.png'} alt={post.data.title} class="w-full h-full object-cover" />
              </div>

              <!-- 右側文字內容 -->
              <div class="flex-1">
                <h3 class="text-base sm:text-lg font-bold text-[var(--text-primary)] mb-1 line-clamp-2">
                  <a href={`/blog/${post.slug}`} class="hover:text-[var(--accent-color)] transition-colors">
                    {post.data.title}
                  </a>
                </h3>
                <p class="text-sm text-[var(--text-secondary)] line-clamp-2 mb-2">
                  {post.data.description}
                </p>

                <!-- 日期 + 前3個 tags + 閱讀按鈕 -->
                <div class="flex flex-wrap items-center justify-between text-xs text-gray-500 gap-2">
                  <span>{new Date(post.data.publishDate).toLocaleDateString('zh-TW')}</span>
                  <div class="flex flex-wrap gap-1">
                    {post.data.tags?.slice(0, 3).map(tag => (
                      <span class="px-2 py-0.5 rounded-md text-gray-700" style="background-color:#e7f0e2;">{tag}</span>
                    ))}
                  </div>
                  <a href={`/blog/${post.slug}`} class="text-[var(--accent-color)] hover:underline ml-auto">
                    閱讀 →
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>

        <!-- 查看所有文章 -->
        <div class="text-right mt-4">
          <a href="/blog" class="text-[var(--accent-color)] font-semibold hover:underline">
            查看所有文章 →
          </a>
        </div>
      </section>

      <!-- 精選文章區塊 -->
      <section>
        <h2 class="text-3xl font-bold text-[var(--text-primary)] mb-6 pb-3 border-b border-[var(--border-color)]">
          精選文章
        </h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          {featuredPosts.map(post => (
            <article class="bg-[var(--bg-card)] rounded-xl shadow-md hover:shadow-lg transition-all duration-300 overflow-hidden">
              
              <!-- 精選橫式 16:9 heroImage -->
              <div class="w-full aspect-video overflow-hidden">
                <img src={post.data.heroImage || '/images/default.png'} alt={post.data.title} class="w-full h-full object-cover" />
              </div>

              <div class="p-4 sm:p-6">
                <h3 class="text-lg font-bold text-[var(--text-primary)] mb-2 line-clamp-2">
                  <a href={`/blog/${post.slug}`} class="hover:text-[var(--accent-color)] transition-colors">
                    {post.data.title}
                  </a>
                </h3>
                <p class="text-[var(--text-secondary)] text-sm leading-relaxed mb-2 line-clamp-3">
                  {post.data.description}
                </p>

                <!-- 日期 + 前3個 tags + 閱讀按鈕 -->
                <div class="flex flex-wrap items-center justify-between text-xs text-gray-500 gap-2">
                  <span>{new Date(post.data.publishDate).toLocaleDateString('zh-TW')}</span>
                  <div class="flex flex-wrap gap-1">
                    {post.data.tags?.slice(0, 3).map(tag => (
                      <span class="px-2 py-0.5 rounded-md text-gray-700" style="background-color:#e7f0e2;">{tag}</span>
                    ))}
                  </div>
                  <a href={`/blog/${post.slug}`} class="text-[var(--accent-color)] hover:underline ml-auto">
                    閱讀 →
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>

        <!-- 查看更多精選文章 -->
        <div class="text-right mt-4">
          <a href="/category/精選文章" class="text-[var(--accent-color)] font-semibold hover:underline">
            查看更多精選文章 →
          </a>
        </div>
      </section>

    </div>

    <!-- 側邊欄 -->
    <Sidebar />

  </div>
</BaseLayout>
