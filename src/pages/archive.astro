---
// 檔案路徑: src/pages/archive.astro (已修正：分類標籤獨立並連結到分類頁)
import BaseLayout from '../layouts/BaseLayout.astro';
import Sidebar from '../components/Sidebar.astro';
import PostCard from '../components/PostCard.astro'; 
import { getCollection } from 'astro:content';

const allPosts = (await getCollection('blog'))
  .filter(post => !post.data.draft) // 過濾草稿
  .sort(
    (a, b) => new Date(b.data.publishDate).getTime() - new Date(a.data.publishDate).getTime()
  );

const allCategories = [...new Set(allPosts.flatMap(post => post.data.category))];
---
<BaseLayout pageTitle="所有文章" description="KAE的雜記中所有的文章彙整。">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-10 lg:gap-12">
    <div class="md:col-span-2 space-y-12">
      <section>
        <h2 class="text-2xl font-bold text-[var(--text-primary)] mb-6 pb-3 border-b border-[var(--border-color)]">
          所有文章 ({allPosts.length})
        </h2>

        <!-- 
           👇 修改重點 A：分類標籤區塊 (新增)
           這是一個獨立的導覽區塊，點擊後會跳轉到分類頁
        -->
        <div class="mb-8 p-4 bg-[var(--bg-card)] rounded-lg shadow-md">
          <h3 class="text-lg font-bold text-[var(--text-primary)] mb-3">瀏覽所有分類</h3>
          <div class="flex flex-wrap gap-2">
            
            <!-- 連結到 /archive (所有文章) -->
            <a href="/archive" class="text-xs font-medium text-white bg-[var(--accent-color)] py-1 px-3 rounded-full hover:bg-[var(--accent-hover)] transition-colors flex-shrink-0">
              所有文章
            </a>
            
            <!-- 連結到各個分類頁面 -->
            {allCategories.map(cat => (
              <a 
                href={`/category/${cat}`} 
                class="text-xs font-medium text-[var(--text-secondary)] bg-gray-100 py-1 px-3 rounded-full hover:bg-gray-200 transition-colors flex-shrink-0"
              >
                {cat}
              </a>
            ))}
          </div>
        </div>

        <!-- 
           👇 修改重點 B：搜尋器區塊 (原本的篩選器，現在只剩下搜尋)
        -->
        <div class="mb-8 p-4 bg-[var(--bg-card)] rounded-lg shadow-md">
          <div class="mb-2">
            <input 
              type="search" 
              id="search-input" 
              placeholder="在此頁面中搜尋..."
              class="w-full px-3 py-2 rounded-md border border-[var(--border-color)] text-sm focus:outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
            >
          </div>
          <div class="text-xs text-[var(--text-secondary)]">請在上方搜尋框輸入關鍵字進行站內搜尋。</div>
        </div>
        
        <div id="no-results-message" class="hidden text-center text-gray-500 py-10">
          <p>找不到符合條件的文章 😅</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6" id="article-list-container">
          {allPosts.map(post => (
            <div 
              class="article-card-wrapper"
              data-search-text={[
                post.data.title,
                post.data.description,
                ...post.data.category,
                ...(post.data.tags || [])
              ].join(' ').toLowerCase()}
              data-category={post.data.category.join(' ')}
            >
              <PostCard post={post} />
            </div>
          ))}
        </div>

      </section>
    </div>
    <Sidebar />
  </div>

  <script>
    // 讓 <head> 中的 ViewTransitions API 知道這個頁面有動態內容
    document.addEventListener('astro:page-load', () => {
      
      const searchInput = document.getElementById('search-input');
      const articleWrappers = document.querySelectorAll('.article-card-wrapper');
      const noResultsMessage = document.getElementById('no-results-message');

      if (!searchInput) return;

      // 由於分類按鈕現在是跳轉頁面，我們只需要專注於搜尋功能
      let currentSearch = '';
      
      // 讀取 URL 參數 (來自頁首的搜尋)
      const urlParams = new URLSearchParams(window.location.search);
      const queryFromUrl = urlParams.get('q');

      if (queryFromUrl) {
        (searchInput as HTMLInputElement).value = queryFromUrl;
        currentSearch = queryFromUrl.toLowerCase().trim();
      }

      function runFilter() {
        let visibleCount = 0;
        const searchTerm = currentSearch; // 使用 currentSearch 變數

        articleWrappers.forEach(wrapper => {
          const el = wrapper as HTMLElement;
          const searchText = el.dataset.searchText || '';

          // 條件: 搜尋文字
          const matchesSearch = searchTerm === '' || searchText.includes(searchTerm);

          if (matchesSearch) {
            el.style.display = 'block'; 
            visibleCount++;
          } else {
            el.style.display = 'none';
          }
        });

        if (noResultsMessage) {
          noResultsMessage.style.display = visibleCount === 0 ? 'block' : 'none';
        }
      }

      // 監聽搜尋框輸入
      searchInput.addEventListener('input', (e) => {
        currentSearch = (e.target as HTMLInputElement).value.toLowerCase().trim();
        runFilter();
      });

      // 頁面載入時，立即執行一次篩選 (處理 URL 帶來的關鍵字)
      runFilter();
    });
  </script>
</BaseLayout>
